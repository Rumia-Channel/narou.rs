2026-05-27: Windows EPUB conversion now treats interrobang-like punctuation (`‼`, `⁇`, `⁈`, `⁉`) and variation selectors (`U+FE0E`, `U+FE0F`) as AozoraEpub3 risky filename characters. These paths use the existing temporary safe `input.txt` workspace and are moved back to the original Unicode filename after conversion. This addresses titles ending with `⁉︎` such as n2111dm when `normalize-filename=false`. `COMMANDS.md` convert implementation notes were updated.