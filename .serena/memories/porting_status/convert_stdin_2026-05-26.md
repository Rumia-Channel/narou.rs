2026-05-26: `narou convert` now reads whitespace-separated targets from non-TTY stdin and appends them after CLI targets, matching narou.rb `CommandLine.run` pipe behavior for `narou list ... | narou convert`. `COMMANDS.md` convert target row and implementation notes were updated. Verification: `cargo test --bin narou_rs`, `cargo check`, and a PowerShell pipe smoke test in `sample/novel` with `('999999 888888' -split ' ') | target/debug/narou_rs.exe convert --no-open --no-epub`.
