2026-05-30: Windows EPUB conversion now treats paths containing CP932/Windows-31J unencodable characters as AozoraEpub3-risky. `src/converter/device.rs` checks `encoding_rs::SHIFT_JIS.encode(...).had_errors` in addition to the existing mapping-risky character list (`〜`, `～`, `−`, interrobang-like marks, variation selectors, etc.). Risky paths use the temporary `input.txt` Aozora workspace and then move the generated EPUB back to the original Unicode filename. Tests cover symbols such as `♠♡♢♣`, `𠮷`, and ensure `①` is not treated as risky. Commit: `d286e6c Handle CP932-unsafe Aozora paths` on `develop`; pushed to `origin/develop`. `COMMANDS.md` convert notes were updated.