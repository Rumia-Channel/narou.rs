# Git Branching Policy

- 大幅な変更や新機能は `develop` を統合先とする。
- 作業開始時は `develop` から機能単位のブランチを作成する。
- 機能ブランチでは、変更内容に応じた適切な動作テストを済ませてから `develop` に統合する。
- `develop` へ統合した後、`develop` 上で必要なテストを済ませてから `main` に統合する。
- 以降の作業では、直接 `main` に機能変更を入れず、この運用フローを前提にする。
